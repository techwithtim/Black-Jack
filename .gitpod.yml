image: gitpod/workspace-full-vnc
ports:
  - port: 5900
    onOpen: ignore
  - port: 6080
    onOpen: open-preview
tasks:
  - init: pip3 install -r requirements.txt
    init: sudo apt-get install python3-tk
    command: python3 main.py
